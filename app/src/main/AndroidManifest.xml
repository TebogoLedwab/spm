<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- FIXED: Added required internet permissions for Google/Firebase authentication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- FIXED: Added package visibility query so your app can see Google Play Services (Required for Android 11+) -->
    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
            android:allowBackup="true"
            android:fullBackupContent="@xml/backup_rules"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:label="@string/app_name"
            android:icon="@drawable/pantry_pal_logo"
            android:roundIcon="@drawable/pantry_pal_logo"
            android:supportsRtl="true"
            android:theme="@style/Theme.Layout">

        <!-- THIS REGISTERS YOUR MAIN SCREEN -->
        <activity
                android:name=".authentication.SignIn"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".authentication.SignUp" />
        <activity android:name=".pages.Pantry" />
        <activity android:name=".pages.AddIngredients" />
        <activity android:name=".pages.Recipe" />
        <activity android:name=".pages.RecipeDetail" />





    </application>

</manifest>
